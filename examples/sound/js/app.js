(function () {
    var app = angular.module('Example', []);
    app.directive('noise', function () {
        return {
            link:function(scope, elem, attr) {
            }
        }
    });
}());
